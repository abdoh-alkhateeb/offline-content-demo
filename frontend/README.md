# Frontend Setup

## Run the Frontend

1. Navigate to the frontend directory:
   ```sh
   cd path/to/frontend
   ```
2. Install dependencies:
   ```sh
   npm install
   ```
3. Build the project:
   ```sh
   npm run build
   ```
4. Serve the app:
   ```sh
   npm run serve
   ```
